const faker = require('faker');
const postgresQuery = require('./postgresQuery.js');




let batchSize = 10000;

const createShoes = (batchSize) => {
  for (let i = 0; i < batchSize; i++) {
    
  }
}

// name: String,
// picture: String,
// id: Number,
// price: Number,
// type: String